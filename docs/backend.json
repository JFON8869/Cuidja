
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Mercado Local Cuidja platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "name": {
          "type": "string",
          "description": "The user's full name."
        },
        "email": {
          "type": "string",
          "description": "The user's email address.",
          "format": "email"
        },
        "phone": {
          "type": "string",
          "description": "The user's phone number."
        },
        "isSeller": {
          "type": "boolean",
          "description": "Indicates whether the user is a seller."
        },
        "addresses": {
          "type": "array",
          "description": "A list of the user's saved addresses.",
          "items": {
            "$ref": "#/backend/entities/Address"
          }
        }
      },
      "required": [
        "id",
        "name",
        "email"
      ]
    },
    "Address": {
      "title": "Address",
      "type": "object",
      "description": "Represents a user's saved address or a shipping address.",
      "properties": {
        "nickname": {
          "type": "string",
          "description": "A nickname for a saved address (e.g., Casa, Trabalho)."
        },
        "zip": {
          "type": "string"
        },
        "street": {
          "type": "string"
        },
        "number": {
          "type": "string"
        },
        "neighborhood": {
          "type": "string"
        },
        "city": {
          "type": "string"
        },
        "name": {
          "type": "string",
          "description": "Recipient's name for a shipping address."
        }
      },
      "required": [
        "street",
        "number",
        "city",
        "zip"
      ]
    },
    "Store": {
      "title": "Store",
      "type": "object",
      "description": "Represents a seller's store.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string" },
        "name": { "type": "string" }
      },
      "required": ["id", "userId", "name"]
    },
    "Product": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Product",
      "type": "object",
      "description": "Represents a product or service listed on the Mercado Local Cuidja platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Product entity."
        },
        "sellerId": {
          "type": "string",
          "description": "Reference to User (Seller). (Relationship: User 1:N Product)"
        },
        "name": {
          "type": "string",
          "description": "The name of the product or service."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the product or service."
        },
        "price": {
          "type": "number",
          "description": "The price of the product or service."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the product image."
        },
        "category": {
          "type": "string",
          "description": "Category of the product (e.g., 'Fruits', 'Vegetables', 'Handicrafts')."
        },
        "location": {
          "type": "string",
          "description": "Location where the product is available."
        },
        "addons": {
          "type": "array",
          "description": "List of add-on groups for the product.",
          "items": {
            "$ref": "#/backend/entities/AddonGroup"
          }
        }
      },
      "required": [
        "id",
        "sellerId",
        "name",
        "description",
        "price",
        "imageUrl",
        "category",
        "location"
      ]
    },
     "Service": {
      "title": "Service",
      "type": "object",
      "description": "Represents a service offered by a provider.",
      "properties": {
        "id": { "type": "string" },
        "providerId": { "type": "string" },
        "sellerId": { "type": "string"},
        "name": { "type": "string" },
        "description": { "type": "string" },
        "category": { "enum": ["Serviços"] },
        "visitFee": { "type": "number", "description": "Optional fee for technical visit or initial contact." }
      },
      "required": ["id", "providerId", "name", "description", "category", "sellerId"]
    },
    "AddonGroup": {
      "title": "AddonGroup",
      "type": "object",
      "description": "A group of add-on options for a product.",
      "properties": {
        "id": { "type": "string" },
        "title": { "type": "string" },
        "type": { "enum": ["single", "multiple"] },
        "addons": {
          "type": "array",
          "items": { "$ref": "#/backend/entities/Addon" }
        }
      },
      "required": ["id", "title", "type", "addons"]
    },
    "Addon": {
      "title": "Addon",
      "type": "object",
      "description": "A single add-on option for a product.",
      "properties": {
        "name": { "type": "string" },
        "price": { "type": "number" }
      },
      "required": ["name", "price"]
    },
    "Order": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Order",
      "type": "object",
      "description": "Represents a product order placed on the platform.",
      "properties": {
        "id": {
          "type": "string"
        },
        "customerId": {
          "type": "string"
        },
        "storeId": {
            "type": "string"
        },
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/backend/entities/OrderItem"
          }
        },
        "orderDate": {
          "type": "string",
          "format": "date-time"
        },
        "totalAmount": {
          "type": "number"
        },
        "status": {
          "type": "string",
          "enum": ["Pendente", "Confirmado", "Em Preparo", "Saiu para Entrega", "Entregue", "Cancelado"]
        },
        "isUrgent": {
          "type": "boolean"
        },
        "shippingAddress": {
            "$ref": "#/backend/entities/Address"
        },
        "phone": {
            "type": "string"
        },
        "sellerHasUnread": { "type": "boolean" },
        "buyerHasUnread": { "type": "boolean" }
      },
      "required": [
        "id",
        "customerId",
        "storeId",
        "items",
        "orderDate",
        "totalAmount",
        "status",
        "shippingAddress"
      ]
    },
    "ServiceRequest": {
        "title": "Service Request",
        "type": "object",
        "description": "Represents a customer's request for a service.",
        "properties": {
            "id": { "type": "string" },
            "requesterId": { "type": "string" },
            "providerId": { "type": "string" },
            "storeId": { "type": "string" },
            "serviceId": { "type": "string" },
            "serviceName": { "type": "string"},
            "requesterInfo": {
                "$ref": "#/backend/entities/Address"
            },
            "message": { "type": "string" },
            "status": { "type": "string", "enum": ["Pendente", "Contatado", "Agendado", "Concluído", "Cancelado"] },
            "requestDate": { "type": "string", "format": "date-time" },
            "providerHasUnread": { "type": "boolean" },
            "requesterHasUnread": { "type": "boolean" }
        },
        "required": ["id", "requesterId", "providerId", "storeId", "serviceId", "status", "requestDate"]
    },
    "OrderItem": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "OrderItem",
      "type": "object",
      "description": "Represents an item within an order.",
      "properties": {
        "id": {
          "type": "string"
        },
        "name": {
            "type": "string"
        },
        "price": {
          "type": "number"
        },
        "quantity": {
          "type": "number"
        },
        "selectedAddons": {
          "type": "array",
          "items": { "$ref": "#/backend/entities/Addon" }
        }
      },
      "required": [
        "id",
        "name",
        "quantity",
        "price"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user information. Security is based on the user ID (path-based ownership).",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "stores/{storeId}",
        "definition": {
            "entityName": "Store",
            "schema": { "$ref": "#/backend/entities/Store" },
            "description": "Stores public information about a seller's store.",
            "params": [
                {
                    "name": "storeId",
                    "description": "The unique identifier for the store."
                }
            ]
        }
      },
      {
        "path": "products/{productId}",
        "definition": {
          "entityName": "Product",
          "schema": {
            "$ref": "#/backend/entities/Product"
          },
          "description": "Stores all products available on the platform. Includes 'sellerId' for authorization independence.",
          "params": [
            {
              "name": "productId",
              "description": "The unique identifier for the product."
            }
          ]
        }
      },
       {
        "path": "services/{serviceId}",
        "definition": {
          "entityName": "Service",
          "schema": { "$ref": "#/backend/entities/Service" },
          "description": "Stores all services available on the platform.",
          "params": [{ "name": "serviceId", "description": "The unique identifier for the service." }]
        }
      },
      {
        "path": "orders/{orderId}",
        "definition": {
          "entityName": "Order",
          "schema": {
            "$ref": "#/backend/entities/Order"
          },
          "description": "Stores all product orders placed on the platform.",
          "params": [
            {
              "name": "orderId",
              "description": "The unique identifier for the order."
            }
          ]
        }
      },
      {
        "path": "serviceRequests/{requestId}",
        "definition": {
          "entityName": "ServiceRequest",
          "schema": { "$ref": "#/backend/entities/ServiceRequest" },
          "description": "Stores all service requests placed on the platform.",
          "params": [{ "name": "requestId", "description": "The unique identifier for the service request." }]
        }
      }
    ],
    "reasoning": "A estrutura foi simplificada para separar claramente Pedidos de Produtos de Solicitações de Serviço. A coleção `orders` agora lida exclusivamente com transações de produtos, contendo `items` e `shippingAddress`. Uma nova coleção `serviceRequests` foi criada para gerenciar solicitações de serviço, com campos como `requesterId`, `providerId`, e `requesterInfo`. Essa separação remove a complexidade e a ambiguidade, tornando os fluxos de dados mais claros e as regras de segurança mais simples de implementar para cada tipo de transação."
  }
}

    